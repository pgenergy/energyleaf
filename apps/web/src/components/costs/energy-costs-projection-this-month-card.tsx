import { Card, CardContent, CardHeader, CardTitle } from "@energyleaf/ui/card";

export default function EnergyCostsProjectionMonth() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Hochrechnung des aktuellen Monats</CardTitle>
            </CardHeader>
        </Card>
    );
}
